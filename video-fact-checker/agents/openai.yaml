interface:
  display_name: "视频事实审查"
  short_description: "核验视频选题、数字与引语，生成可追溯的脚本事实卡"
  default_prompt: "Use $video-fact-checker to verify this video topic with a temporal source sweep, trace secondary claims upstream, scope every negative finding, fully recheck revisions, and produce a validated traceable fact card."
policy:
  allow_implicit_invocation: true
